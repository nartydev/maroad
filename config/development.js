const {
	RECAPTCHA_PUBLIC,
	SITE_NAME,
	SERVER_HOST,
	PRISMIC_REPEATABLE,
	PRISMIC_SINGLE,
	CSS_VARIABLES
} = require('./production.js');

module.exports = {
	HOST: 'localhost',
	FORCE_HTTPS: 0,

	SSL_CERTIFICATE_FILE: null,
	SSL_CERTIFICATE_KEY_FILE: null,

	RECAPTCHA_PUBLIC,

	SERVER_HOST,

	SITE_NAME,

	NOTIFICATION_PREFIX: 'DEVELOPMENT [maroad]',

	CSS_VARIABLES
};
